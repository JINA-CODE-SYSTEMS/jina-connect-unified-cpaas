---
title: Team Inbox API
description: Real-time shared inbox with WebSocket support, cross-channel messaging.
---

import { Aside } from "@astrojs/starlight/components";

## Overview

The Team Inbox provides a unified, real-time shared inbox across all messaging channels. Agents can view, reply to, and manage conversations from WhatsApp, Telegram, SMS, and RCS in one place.

## REST Endpoints

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/team-inbox/api/messages/` | List inbox messages |
| `GET` | `/team-inbox/api/messages/{id}/` | Get message details |
| `GET` | `/team-inbox/api/stats/` | Inbox statistics |
| `GET` | `/team-inbox/api/export-pdf/` | Export conversation as PDF |
| `GET` | `/team-inbox/websocket/info/` | WebSocket connection info |

## WebSocket Connection

Connect to the real-time inbox:

```
ws://your-domain.com/ws/team-inbox/{tenant_id}/
```

### Authentication

Pass JWT token as a query parameter:

```
ws://your-domain.com/ws/team-inbox/{tenant_id}/?token=eyJhbGci...
```

### WebSocket Events

**Incoming events (server → client):**

| Event | Description |
|---|---|
| `new_message` | New message received from any channel |
| `new_event` | Timeline event (ticket opened, assigned, closed) |
| `message_status_update` | Delivery status update (`SENT`, `DELIVERED`, `READ`, `FAILED`) |
| `messages_read` | Another agent marked messages read |
| `typing_indicator` | Contact or agent is typing |
| `assignment_update` | Contact assigned/reassigned |
| `payment_status_update` | Order/payment status changed |
| `mention_notification` | An agent was @-mentioned |
| `error` | The server rejected a client message |

**Outgoing events (client → server):**

| Event | Description |
|---|---|
| `mark_as_read` | Mark messages as read |
| `get_timeline` | Fetch a contact's message timeline |
| `get_chat_list` | Fetch the conversation list |
| `typing_indicator` | Send typing indicator |
| `client_info` | Declare client type |

Replies are posted over REST, not the socket. The authoritative list is
served live from `/team-inbox/websocket/info/`.

### Correlating a broadcast bubble

`POST`ing a broadcast answers `201` before anything is sent, so a client that
draws an optimistic pending bubble per recipient needs a way to match the
inbox row that follows. Body text does not work — two identical templates to
one contact render the same string — so every inbox row created by a broadcast
carries its identity in `content._meta`:

```json
{
  "source": "broadcast",
  "broadcast_id": 7,
  "broadcast_message_id": 42,
  "contact_id": 789
}
```

`broadcast_message_id` is the per-recipient send and is unique on its own;
`(broadcast_id, contact_id)` is the same identity spelled in what the client
already holds at `201` time. Delivery state is read from `outgoing_status`,
`outgoing_failed_at` and `outgoing_error` on the row, not from `_meta`.

A send that fails before the provider accepts it produces an inbox row with
`outgoing_status: "FAILED"` **and** a `message_status_update` naming the same
identifiers, with `external_message_id: null` (nothing was ever accepted) and
`error` carrying the provider's wording.

## Message Model

Each message in the inbox has:

| Field | Type | Description |
|---|---|---|
| `platform` | enum | `WHATSAPP`, `TELEGRAM`, `SMS`, `RCS`, `VOICE` |
| `direction` | enum | `INCOMING`, `OUTGOING` |
| `author_type` | enum | `USER`, `CONTACT`, `BOT` |
| `content` | JSON | Message content (text, media, etc.) |
| `is_read` | boolean | Read by agent |
| `wa_message` | FK | Linked WhatsApp message (for status tracking) |
| `expires_at` | datetime | 24-hour expiry for WhatsApp conversations |

## Statistics

```bash
curl http://localhost:8000/team-inbox/api/stats/ \
  -H "Authorization: Bearer <token>"
```

Returns:
- Total conversations
- Unread count
- Open tickets
- Conversations by channel
- Agent workload distribution

## PDF Export

Export a conversation thread as a formatted PDF:

```bash
curl http://localhost:8000/team-inbox/api/export-pdf/?contact_id={id} \
  -H "Authorization: Bearer <token>" \
  -o conversation.pdf
```

## Configuration

| Setting | Default | Description |
|---|---|---|
| `WEBSOCKET_ACCEPT_ALL` | `True` | Accept WebSocket connections from all origins |
| `WEBSOCKET_TOKEN_EXPIRY` | `3600` | WebSocket token expiry (seconds) |
| `TEAM_INBOX_MESSAGE_LIMIT` | — | Max messages per query |
| `TEAM_INBOX_TYPING_TIMEOUT` | — | Typing indicator timeout |

<Aside type="tip">
  Test WebSocket connections at `http://localhost:8000/team-inbox/websocket/test/` — a built-in testing page for debugging.
</Aside>
